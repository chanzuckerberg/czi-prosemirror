language: node_js
node_js:
  - node
jobs:
  include: 
    - stage: Lint
      script: npm run lint
      name: Linting JS Files
    - stage: "Type Checking" 
      script: npx flow check
    - stage: "Build Dist"
      script: npm run dist:build
